<UserControl x:Class="GymFinanceBillingWpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock x:Name="TxtWelcome" Text="Welcome back, Admin" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
            <TextBlock Text="Here is what's happening at AeroGym today." FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- KPI Metrics Cards (Grid) -->
        <UniformGrid Grid.Row="1" x:Name="KpiGrid" Columns="5" Margin="0,0,0,32">
            <!-- Active Members -->
            <Border x:Name="BorderActiveMembers" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="ACTIVE MEMBERS" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock x:Name="TxtActiveMembers" Text="--" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,0" />
                    <TextBlock Text="Currently registered" FontSize="11" Foreground="{StaticResource Secondary}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Monthly Revenue -->
            <Border x:Name="BorderRevenue" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="MONTHLY REVENUE" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock x:Name="TxtMonthlyRevenue" Text="--" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,0" />
                    <TextBlock Text="Paid invoices this month" FontSize="11" Foreground="{StaticResource StatusPaid}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Unpaid Dues -->
            <Border x:Name="BorderDues" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="UNPAID DUES" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock x:Name="TxtUnpaidDues" Text="--" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,0" />
                    <TextBlock Text="Pending payment" FontSize="11" Foreground="{StaticResource StatusUnpaid}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Expenses -->
            <Border x:Name="BorderExpenses" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="MONTHLY EXPENSES" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock x:Name="TxtMonthlyExpenses" Text="--" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,0" />
                    <TextBlock Text="Utilities, rent &amp; salaries" FontSize="11" Foreground="{StaticResource StatusOverdue}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Net Profit -->
            <Border x:Name="BorderProfit" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="NET PROFIT" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock x:Name="TxtNetProfit" Text="--" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,0" />
                    <TextBlock Text="Revenue - Expenses" FontSize="11" Foreground="{DynamicResource PrimaryLight}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Details Section -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ColRenewals" Width="*" />
                <ColumnDefinition x:Name="ColTransactions" Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Upcoming Renewals (Left Card) -->
            <Border x:Name="BorderRenewals" Grid.Column="0" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="24" Margin="0,0,16,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Upcoming Member Renewals" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16" />

                    <DataGrid Grid.Row="1" x:Name="GridRenewals" Style="{StaticResource DataGridPremium}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Member" Binding="{Binding FullName}" Width="*" />
                            <DataGridTextColumn Header="Active Plan" Binding="{Binding ActivePlan.Name}" Width="150" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="Active">
                                                <Setter Property="Foreground" Value="{StaticResource StatusPaid}"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="Expired">
                                                <Setter Property="Foreground" Value="{StaticResource StatusOverdue}"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="Suspended">
                                                <Setter Property="Foreground" Value="{StaticResource StatusUnpaid}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Recent Invoices / Transactions (Right Card) -->
            <Border x:Name="BorderTransactions" Grid.Column="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Recent Transactions" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16" />

                    <DataGrid Grid.Row="1" x:Name="GridTransactions" Style="{StaticResource DataGridPremium}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNumber}" Width="110" />
                            <DataGridTextColumn Header="Member" Binding="{Binding Member.FullName}" Width="*" />
                            <DataGridTextColumn Header="Amount" Binding="{Binding Total, StringFormat={}₹{0:N2}}" Width="90" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="Paid">
                                                <Setter Property="Foreground" Value="{StaticResource StatusPaid}"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="Unpaid">
                                                <Setter Property="Foreground" Value="{StaticResource StatusUnpaid}"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="Overdue">
                                                <Setter Property="Foreground" Value="{StaticResource StatusOverdue}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>


