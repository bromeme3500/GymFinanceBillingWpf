<UserControl x:Class="GymFinanceBillingWpf.Views.ExpensesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.2*" />
            <ColumnDefinition Width="1.8*" />
        </Grid.ColumnDefinitions>

        <!-- Left Side: Expenses Ledger -->
        <Grid Grid.Column="0" Margin="0,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Page Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Gym Expenses Log" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                <TextBlock Text="Track utilities, trainer commissions, rent and maintenance payments." FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0" />
            </StackPanel>

            <!-- Search and Filter Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtSearch" Style="{StaticResource TextBoxGlass}" Tag="Search by description..." TextChanged="TxtSearch_TextChanged" Margin="0,0,12,0" />
                <ComboBox Grid.Column="1" x:Name="ComboCategoryFilter" Height="36" SelectionChanged="ComboCategoryFilter_SelectionChanged" Margin="0,0,12,0" Background="#E0050810" Foreground="Black" />
                <Button Grid.Column="2" Content="Reset" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnReset_Click" Height="36" />
            </Grid>

            <!-- Expenses Data Grid -->
            <DataGrid Grid.Row="2" x:Name="GridExpenses" Style="{StaticResource DataGridPremium}" SelectionChanged="GridExpenses_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="100" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}₹{0:N2}}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Side: Expense Editor Panel -->
        <Border Grid.Column="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="24">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="TxtPanelTitle" Text="Record New Expense" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20" />

                    <!-- Form Inputs -->
                    <TextBlock Text="Expense Description" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtDescription" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,16" Tag="e.g. Rent June, Electric Bill" />

                    <TextBlock Text="Amount (₹)" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtAmount" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,16" Tag="e.g. 150.00" />

                    <TextBlock Text="Date" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <DatePicker x:Name="DpDate" Height="36" Margin="0,0,0,16" Background="#E0050810" />

                    <TextBlock Text="Category" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox x:Name="ComboCategory" Height="36" Margin="0,0,0,16" Background="#E0050810" Foreground="Black">
                        <ComboBoxItem Content="Utilities" IsSelected="True" />
                        <ComboBoxItem Content="Rent" />
                        <ComboBoxItem Content="Salaries" />
                        <ComboBoxItem Content="Equipment" />
                        <ComboBoxItem Content="Marketing" />
                        <ComboBoxItem Content="Other" />
                    </ComboBox>

                    <TextBlock Text="Notes" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtNotes" Style="{StaticResource TextBoxGlass}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,24" />

                    <!-- Actions -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Save Expense" Style="{StaticResource ButtonPremium}" Click="BtnSave_Click" Margin="0,0,6,0" />
                        <Button Grid.Column="1" Content="Clear Form" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnClear_Click" Margin="6,0,0,0" />
                    </Grid>

                    <Button x:Name="BtnDelete" Content="Delete Expense" Style="{StaticResource ButtonPremiumDanger}" Click="BtnDelete_Click" Margin="0,12,0,0" Visibility="Collapsed" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>


