<UserControl x:Class="GymFinanceBillingWpf.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.1*" />
            <ColumnDefinition Width="1.9*" />
        </Grid.ColumnDefinitions>

        <!-- Left Side: Invoices Directory -->
        <Grid Grid.Column="0" Margin="0,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Page Title -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Invoices &amp; Billing Ledger" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                    <TextBlock Text="Track gym revenue, pending fees, and transaction history." FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="+ New Invoice" Style="{StaticResource ButtonPremium}" Click="BtnNewInvoice_Click" Height="36" VerticalAlignment="Center" />
            </Grid>

            <!-- Search and Filter Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtSearch" Style="{StaticResource TextBoxGlass}" Tag="Search by member..." TextChanged="TxtSearch_TextChanged" Margin="0,0,12,0" />
                <ComboBox Grid.Column="1" x:Name="ComboStatusFilter" Height="36" SelectionChanged="ComboStatusFilter_SelectionChanged" Margin="0,0,12,0" Background="#E0050810" Foreground="Black" />
                <Button Grid.Column="2" Content="Reset" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnReset_Click" Height="36" />
            </Grid>

            <!-- Invoices Data Grid -->
            <DataGrid Grid.Row="2" x:Name="GridInvoices" Style="{StaticResource DataGridPremium}" SelectionChanged="GridInvoices_SelectionChanged" HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Slip No" Binding="{Binding InvoiceNumber}" Width="100" />
                    <DataGridTextColumn Header="Reg No" Binding="{Binding Member.RegNo}" Width="80" />
                    <DataGridTextColumn Header="Customer Name" Binding="{Binding Member.FullName}" Width="150" />
                    <DataGridTextColumn Header="Mobile No" Binding="{Binding Member.Phone}" Width="100" />
                    <DataGridTextColumn Header="Slip Date" Binding="{Binding IssueDate, StringFormat={}{0:yyyy-MM-dd}}" Width="90" />
                    <DataGridTextColumn Header="Due From" Binding="{Binding ServicePeriodStart, StringFormat={}{0:yyyy-MM-dd}}" Width="90" />
                    <DataGridTextColumn Header="Due To" Binding="{Binding ServicePeriodEnd, StringFormat={}{0:yyyy-MM-dd}}" Width="90" />
                    <DataGridTextColumn Header="Adm Fee" Binding="{Binding AdmissionFee, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="Adm Disc" Binding="{Binding AdmissionFeeDiscount, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="Fee" Binding="{Binding Total, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="Grand Total" Binding="{Binding GrandTotal, StringFormat={}₹{0:N2}}" Width="100" />
                    <DataGridTextColumn Header="Cash Recd" Binding="{Binding CashAmount, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="UPI Amt" Binding="{Binding UpiAmount, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="Card Amt" Binding="{Binding CardAmount, StringFormat={}₹{0:N2}}" Width="90" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Paid">
                                        <Setter Property="Foreground" Value="{StaticResource StatusPaid}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Unpaid">
                                        <Setter Property="Foreground" Value="{StaticResource StatusUnpaid}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Overdue">
                                        <Setter Property="Foreground" Value="{StaticResource StatusOverdue}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Side: Invoice Viewer / Payment Panel -->
        <Border Grid.Column="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20">
            <Grid>
                <!-- Empty State Message -->
                <TextBlock x:Name="TxtEmptyState" Text="Select an invoice from the ledger to view details or record payments." FontSize="14" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />

                <!-- Invoice Details View -->
                <ScrollViewer x:Name="ScrollInvoiceDetails" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                    <StackPanel>
                        <!-- Invoice Header -->
                        <TextBlock x:Name="TxtInvoiceNum" Text="INV-2026-000" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                        <TextBlock x:Name="TxtInvoiceDate" Text="Issued: June 18, 2026" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,12" />

                        <Separator Background="{DynamicResource BorderMain}" Margin="0,0,0,12" />

                        <!-- Member Info -->
                        <TextBlock Text="BILL TO" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <TextBlock x:Name="TxtMemberName" Text="Member Name" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" />
                        <TextBlock x:Name="TxtMemberContact" Text="Email / Phone" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,2,0,12" />

                        <!-- Service Period -->
                        <TextBlock Text="SERVICE PERIOD" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <TextBlock x:Name="TxtServicePeriod" Text="Due From - Due To" FontSize="13" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12" />

                        <!-- Status and Payment details -->
                        <TextBlock Text="INVOICE STATUS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <Border x:Name="BorderStatus" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,12">
                            <TextBlock x:Name="TxtStatus" Text="Unpaid" FontWeight="SemiBold" />
                        </Border>

                        <Separator Background="{DynamicResource BorderMain}" Margin="0,0,0,12" />

                        <!-- Items List -->
                        <TextBlock Text="ITEMS SUMMARY" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8" />
                        <ItemsControl x:Name="ListItems">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Description}" Foreground="{DynamicResource TextPrimary}" FontSize="13" TextWrapping="Wrap" />
                                        <TextBlock Grid.Column="1" Text="{Binding TotalPrice, StringFormat={}₹{0:N2}}" Foreground="{DynamicResource TextPrimary}" FontSize="13" FontWeight="SemiBold" Margin="12,0,0,0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Narration -->
                        <TextBlock x:Name="LblNarration" Text="NARRATION" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,8,0,4" Visibility="Collapsed" />
                        <TextBlock x:Name="TxtNarration" Text="--" FontSize="13" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12" TextWrapping="Wrap" Visibility="Collapsed" />

                        <Separator Background="{DynamicResource BorderMain}" Margin="0,4,0,12" />

                        <!-- Summary grid showing admission and split values -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Fee Subtotal:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtSubtotal" Text="₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Admission Fee:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtAdmission" Text="₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Admission Discount:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtDiscount" Text="-₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Grand Total:" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8" />
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtGrandTotal" Text="₹0.00" FontSize="15" FontWeight="Bold" Foreground="{StaticResource Secondary}" HorizontalAlignment="Right" Margin="0,0,0,8" />

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Cash Received:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TxtCashPaid" Text="₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="UPI Received:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="TxtUpiPaid" Text="₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Card Received:" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                            <TextBlock Grid.Row="6" Grid.Column="1" x:Name="TxtCardPaid" Text="₹0.00" FontSize="13" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right" Margin="0,0,0,4" />
                        </Grid>

                        <Separator Background="{DynamicResource BorderMain}" Margin="0,0,0,12" />

                        <!-- Amount Due -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Total Amount Due" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" x:Name="TxtInvoiceTotal" Text="₹0.00" Foreground="{StaticResource StatusOverdue}" FontSize="18" FontWeight="Bold" />
                        </Grid>

                        <!-- Payment Section (if unpaid/overdue) -->
                        <StackPanel x:Name="PanelPayment" Margin="0,0,0,16">
                            <TextBlock Text="RECORD PAYMENT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,6" />
                            <!-- Split input details on payment receipt -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock Text="Cash Recd" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtRecordCash" Style="{StaticResource TextBoxGlass}" Text="0.00" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,4,0">
                                    <TextBlock Text="UPI Amt" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtRecordUpi" Style="{StaticResource TextBoxGlass}" Text="0.00" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="4,0,0,0">
                                    <TextBlock Text="Card Amt" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtRecordCard" Style="{StaticResource TextBoxGlass}" Text="0.00" />
                                </StackPanel>
                            </Grid>
                            <Button Content="Confirm Payment Received" Style="{StaticResource ButtonPremium}" Click="BtnConfirmPayment_Click" Height="38" />
                        </StackPanel>

                        <Separator Background="{DynamicResource BorderMain}" Margin="0,8,0,16" />

                        <!-- Actions -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Edit Invoice" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnEditInvoice_Click" Margin="0,0,6,0" />
                            <Button Grid.Column="1" Content="Delete" Style="{StaticResource ButtonPremiumDanger}" Click="BtnDeleteInvoice_Click" Margin="6,0,0,0" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
