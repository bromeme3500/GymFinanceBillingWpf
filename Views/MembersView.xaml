<UserControl x:Class="GymFinanceBillingWpf.Views.MembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColDirectory" Width="*" />
            <ColumnDefinition x:Name="ColEditor" Width="0" />
        </Grid.ColumnDefinitions>

        <!-- Left Side: Member Directory -->
        <Grid Grid.Column="0" Margin="0,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Page Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Gym Members Directory" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                <TextBlock Text="Manage active gym memberships and contact details." FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0" />
            </StackPanel>

            <!-- Search and Filter Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtSearch" Style="{StaticResource TextBoxGlass}" Tag="Search by name, email or phone..." TextChanged="TxtSearch_TextChanged" Margin="0,0,12,0" />
                <Button Grid.Column="1" Content="Reset Search" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnResetSearch_Click" Height="36" Margin="0,0,12,0" />
                <Button Grid.Column="2" Content="+ Add New Member" Style="{StaticResource ButtonPremium}" Click="BtnAddNewMember_Click" Height="36" />
            </Grid>

            <!-- Members Data Grid -->
            <DataGrid Grid.Row="2" x:Name="GridMembers" Style="{StaticResource DataGridPremium}" SelectionChanged="GridMembers_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Reg No" Binding="{Binding RegNo}" Width="70" />
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="100" />
                    <DataGridTextColumn Header="Joined" Binding="{Binding JoinDate, StringFormat={}{0:yyyy-MM-dd}}" Width="90" />
                    <DataGridTextColumn Header="Active Plan" Binding="{Binding ActivePlan.Name}" Width="130" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Active">
                                        <Setter Property="Foreground" Value="{StaticResource StatusPaid}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Expired">
                                        <Setter Property="Foreground" Value="{StaticResource StatusOverdue}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Suspended">
                                        <Setter Property="Foreground" Value="{StaticResource StatusUnpaid}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Side: Member Details / Editor Panel -->
        <Border x:Name="BorderEditor" Grid.Column="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20" Visibility="Collapsed">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="TxtPanelTitle" Grid.Column="0" Text="Register New Member" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
                        <Button Grid.Column="1" Content="✕" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnCloseEditor_Click" VerticalAlignment="Center" Width="30" Height="30" />
                    </Grid>

                    <!-- Two Column Registration Form -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Column 0 (Left Side Fields) -->
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Reg No" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtRegNo" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Full Name" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtFullName" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Email Address" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtEmail" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Phone Number" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtPhone" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Address Line 1" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtAddress1" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Address Line 2" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtAddress2" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Address Line 3" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtAddress3" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />

                            <TextBlock Text="Pin Code" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtPinCode" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />
                        </StackPanel>

                        <!-- Column 1 (Right Side Fields) -->
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Reg. Date" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <DatePicker x:Name="DpRegDate" Height="36" Margin="0,0,0,12" />

                            <TextBlock Text="Joining Date" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <DatePicker x:Name="DpJoinDate" Height="36" Margin="0,0,0,12" />

                            <TextBlock Text="Membership Plan" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <ComboBox x:Name="ComboPlans" Height="36" Margin="0,0,0,12" Background="#E0050810" Foreground="Black" />

                            <TextBlock Text="Status" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <ComboBox x:Name="ComboStatus" Height="36" Margin="0,0,0,12" Background="#E0050810" Foreground="Black" />

                            <TextBlock Text="Gender" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <ComboBox x:Name="ComboGender" Height="36" Margin="0,0,0,12" Background="#E0050810" Foreground="Black">
                                <ComboBoxItem Content="Male" />
                                <ComboBoxItem Content="Female" />
                                <ComboBoxItem Content="Other" />
                            </ComboBox>

                            <TextBlock Text="Blood Group" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <ComboBox x:Name="ComboBloodGroup" Height="36" Margin="0,0,0,12" Background="#E0050810" Foreground="Black">
                                <ComboBoxItem Content="A+" />
                                <ComboBoxItem Content="A-" />
                                <ComboBoxItem Content="B+" />
                                <ComboBoxItem Content="B-" />
                                <ComboBoxItem Content="AB+" />
                                <ComboBoxItem Content="AB-" />
                                <ComboBoxItem Content="O+" />
                                <ComboBoxItem Content="O-" />
                                <ComboBoxItem Content="Unknown" />
                            </ComboBox>

                            <!-- Age, Height, Weight (small horizontal layout) -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock Text="Age" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtAge" Style="{StaticResource TextBoxGlass}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,4,0">
                                    <TextBlock Text="Height (cm)" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtHeight" Style="{StaticResource TextBoxGlass}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="4,0,0,0">
                                    <TextBlock Text="Weight (kg)" Foreground="{DynamicResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBox x:Name="TxtWeight" Style="{StaticResource TextBoxGlass}" />
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Opening Amount" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                            <TextBox x:Name="TxtOpeningAmount" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,12" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Remarks / Notes" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,4,0,6" />
                    <TextBox x:Name="TxtNotes" Style="{StaticResource TextBoxGlass}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16" />

                    <!-- Actions -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Save Member" Style="{StaticResource ButtonPremium}" Click="BtnSave_Click" Margin="0,0,6,0" />
                        <Button Grid.Column="1" Content="Clear Form" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnClear_Click" Margin="6,0,0,0" />
                    </Grid>

                    <Button x:Name="BtnDelete" Content="Delete Member" Style="{StaticResource ButtonPremiumDanger}" Click="BtnDelete_Click" Margin="0,12,0,0" Visibility="Collapsed" />

                    <!-- QR Code Card Panel -->
                    <StackPanel x:Name="PanelQrCode" Margin="0,20,0,0" Visibility="Collapsed">
                        <Separator Background="{DynamicResource BorderMain}" Margin="0,0,0,16" />
                        <TextBlock Text="MEMBER CHECK-IN QR CODE" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8" />
                        <Border BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="8" Padding="12" Background="{DynamicResource BgMain}" Width="160" Height="160" HorizontalAlignment="Center">
                            <Image x:Name="ImgQrCode" Stretch="Uniform" />
                        </Border>
                        <Button Content="Save QR Card" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnSaveQr_Click" Margin="0,12,0,0" Height="36" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>


