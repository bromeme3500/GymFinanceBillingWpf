<UserControl x:Class="GymFinanceBillingWpf.Views.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.2*" />
            <ColumnDefinition Width="1.8*" />
        </Grid.ColumnDefinitions>

        <!-- Left Side: Salary Ledger -->
        <Grid Grid.Column="0" Margin="0,0,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Page Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Staff Payroll Ledger" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                <TextBlock Text="Track salary payments, allowances, deductions and commissions for instructors and receptionists." FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0" />
            </StackPanel>

            <!-- Search and Filter Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtSearch" Style="{StaticResource TextBoxGlass}" Tag="Search by staff name..." TextChanged="TxtSearch_TextChanged" Margin="0,0,12,0" />
                <ComboBox Grid.Column="1" x:Name="ComboMonthFilter" Height="36" SelectionChanged="ComboMonthFilter_SelectionChanged" Margin="0,0,12,0" Background="#E0050810" Foreground="Black" />
                <Button Grid.Column="2" Content="Reset" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnReset_Click" Height="36" />
            </Grid>

            <!-- Payroll Data Grid -->
            <DataGrid Grid.Row="2" x:Name="GridPayroll" Style="{StaticResource DataGridPremium}" SelectionChanged="GridPayroll_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Staff Name" Binding="{Binding StaffName}" Width="*" />
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120" />
                    <DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="100" />
                    <DataGridTextColumn Header="Net Salary" Binding="{Binding NetSalary, StringFormat={}₹{0:N2}}" Width="110" />
                    <DataGridTextColumn Header="Payment Date" Binding="{Binding PaymentDate, StringFormat={}{0:yyyy-MM-dd}}" Width="110" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Paid">
                                        <Setter Property="Foreground" Value="{StaticResource StatusPaid}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Pending">
                                        <Setter Property="Foreground" Value="{StaticResource StatusUnpaid}"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Draft">
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Side: Payroll Editor Panel -->
        <Border Grid.Column="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,0,12,0">
                    <TextBlock x:Name="TxtPanelTitle" Text="Record Staff Payment" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20" />

                    <!-- Form Inputs -->
                    <TextBlock Text="Select Employee / Staff" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox x:Name="ComboStaff" Height="36" Margin="0,0,0,14" Background="#E0050810" Foreground="Black" SelectionChanged="ComboStaff_SelectionChanged" />

                    <TextBlock Text="Salary Month" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox x:Name="ComboMonth" Height="36" Margin="0,0,0,14" Background="#E0050810" Foreground="Black" />

                    <TextBlock Text="Basic Salary (₹)" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtBasicSalary" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,14" Text="0.00" TextChanged="Calculations_TextChanged" />

                    <TextBlock Text="Allowances &amp; Incentives (₹)" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtAllowance" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,14" Text="0.00" TextChanged="Calculations_TextChanged" />

                    <TextBlock Text="Deductions &amp; Taxes (₹)" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtDeduction" Style="{StaticResource TextBoxGlass}" Margin="0,0,0,14" Text="0.00" TextChanged="Calculations_TextChanged" />

                    <TextBlock Text="Net Salary (Auto)" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtNetSalary" Style="{StaticResource TextBoxGlass}" IsReadOnly="True" Background="{DynamicResource BgMain}" Margin="0,0,0,14" Text="0.00" />

                    <TextBlock Text="Payment Date" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <DatePicker x:Name="DpPaymentDate" Height="36" Margin="0,0,0,14" Background="#E0050810" />

                    <TextBlock Text="Payment Mode" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox x:Name="ComboPaymentMethod" Height="36" Margin="0,0,0,14" Background="#E0050810" Foreground="Black">
                        <ComboBoxItem Content="Cash" IsSelected="True" />
                        <ComboBoxItem Content="UPI" />
                        <ComboBoxItem Content="Bank Transfer" />
                        <ComboBoxItem Content="Card" />
                    </ComboBox>

                    <TextBlock Text="Payment Status" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox x:Name="ComboStatus" Height="36" Margin="0,0,0,14" Background="#E0050810" Foreground="Black" />

                    <TextBlock Text="Narration / Description" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="TxtNarration" Style="{StaticResource TextBoxGlass}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20" />

                    <!-- Actions -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Save Record" Style="{StaticResource ButtonPremium}" Click="BtnSave_Click" Margin="0,0,6,0" />
                        <Button Grid.Column="1" Content="Clear Form" Style="{StaticResource ButtonPremiumSecondary}" Click="BtnClear_Click" Margin="6,0,0,0" />
                    </Grid>

                    <Button x:Name="BtnDelete" Content="Delete Record" Style="{StaticResource ButtonPremiumDanger}" Click="BtnDelete_Click" Margin="0,12,0,0" Visibility="Collapsed" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
