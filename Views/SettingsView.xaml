<UserControl x:Class="GymFinanceBillingWpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Settings" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource OutfitFont}" />
            <TextBlock Text="Customize the look and feel of your AeroGym application." FontSize="14" Foreground="{DynamicResource TextSecondary}" FontFamily="{StaticResource OutfitFont}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Settings Content -->
        <Border Grid.Row="1" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderMain}" BorderThickness="1" CornerRadius="12" Padding="28" VerticalAlignment="Top" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel>

                <!-- Section Title 2 -->
                <TextBlock Text="Full Color Schemes" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource OutfitFont}" Margin="0,0,0,6" />
                <TextBlock Text="Choose a full interface palette (background, cards, and text colors)." FontSize="13" Foreground="{DynamicResource TextSecondary}" FontFamily="{StaticResource OutfitFont}" Margin="0,0,0,16" />

                <!-- Theme Selection Buttons -->
                <UniformGrid Columns="4" Margin="0,0,0,24">
                    <Button Click="BtnTheme_Click" Tag="Midnight" Content="Midnight Dark" Style="{StaticResource ButtonPremiumSecondary}" Margin="0,0,8,0" Height="40" />
                    <Button Click="BtnTheme_Click" Tag="Light" Content="Light Minimal" Style="{StaticResource ButtonPremiumSecondary}" Margin="0,0,8,0" Height="40" />
                    <Button Click="BtnTheme_Click" Tag="Cyberpunk" Content="Cyberpunk" Style="{StaticResource ButtonPremiumSecondary}" Margin="0,0,8,0" Height="40" />
                    <Button Click="BtnTheme_Click" Tag="Forest" Content="Forest Moss" Style="{StaticResource ButtonPremiumSecondary}" Margin="0,0,8,0" Height="40" />
                </UniformGrid>

                <!-- Status Feedback -->
                <TextBlock x:Name="TxtStatus" Text="Select a theme above to apply it immediately." FontSize="13" Foreground="{DynamicResource TextSecondary}" FontFamily="{StaticResource OutfitFont}" Height="20" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>


